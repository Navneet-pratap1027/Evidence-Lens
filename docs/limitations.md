# Known Limitations

- Metadata (EXIF/codec/GPS) is often stripped by social platforms on
  re-upload — treated as an optional auxiliary signal, not primary
  temporal verification.
- Deepfake detection accuracy degrades on compressed/re-uploaded
  social media video — kept out of default fusion score.
- Reverse search coverage is limited to what's indexed/reachable via
  the chosen search method.
